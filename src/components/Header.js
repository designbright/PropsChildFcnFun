import React, {Component} from 'react';

export default class Header extends Component {
  render() {
    return (
      <nav className="navBar">
        <a className="navBarlink" href="#">Design Bright's Chat Bot</a>
      </nav>
    );
  }
}
